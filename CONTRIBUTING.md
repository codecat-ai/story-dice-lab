# Contributing

Thanks for considering a contribution.

- Keep the app local-first and dependency-light.
- Add or update behavior tests before changing production behavior.
- Run `npm run lint`, `npm run typecheck`, `npm test -- --run`, and `npm run build` before opening a PR.
- Use English commit messages and code comments.
